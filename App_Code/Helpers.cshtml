@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper, IList<tech4mEntity.Tag> tags)
{
  foreach (var tag in tags)
  {
    <div class="tags-div">@tech4mUI.Extensions.ActionLinkExtensions.TagLink(htmlHelper, tag)</div>     
  }
}